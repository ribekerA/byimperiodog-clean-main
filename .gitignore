# Dependencies
node_modules/

# Next.js / Build
.next/
.next-build/
out/

# TypeScript build info
tsconfig.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.run_dev_log.txt
.run_dev_attempt.sh
.last_action.txt

# Coverage / Reports
coverage/
playwright-report/
reports/a11y-contrast.diff
# Evidências brutas locais da migração (regeráveis e muito volumosas)
reports/migration-next16/screenshots/
reports/migration-next16/*eslint*.json
reports/migration-next16/*lighthouse*.json
reports/route-validation.json

# Env (local secrets)
.env.local
.env.*.local
.env

# Misc generated
.DS_Store
*.swp
*.zip
*.bak

# Backups (already purged)
backup-*.zip
bk.zip
bkp*.zip

# Testing
.test-results/
test-results/

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Contentlayer cache
.contentlayer/

# Temporary
TEMP_*
.vercel

# Local Netlify folder
.netlify

# Local HTTPS dev certs (mkcert)
*.pem

# Claude Code local settings
.claude/

# Arquivo de midia original (fotos e videos de camera, 2022)
# Ficavam soltos em public/filhotes/ e iam para o deploy inteiros — 297,7 MB que
# nenhuma pagina referencia. Saem do repositorio mas CONTINUAM NO DISCO, aqui.
# Para recuperar a versao antiga: git checkout 47cef43 -- public/filhotes
midia-original/

# Diretorio de trabalho local: scripts de patch de uso unico e textos de
# especificacao usados durante uma rodada. Nao e codigo do site.
scratchpad/

# Evidencia bruta de Lighthouse desta rodada: traces e relatorios HTML gerados
# localmente. Sao 18 MB regeraveis com `npx lighthouse`, e o numero que importa
# ja esta escrito em FINAL_CONSOLIDATED_AUDIT.md.
reports/lh-*.json
localhost_*.report.html

# Lockfile de Deno criado por ferramenta avulsa. O projeto e npm + Node 24;
# nada em package.json ou netlify.toml usa Deno.
deno.lock

# Midia recebida que ainda NAO pode ser publicada porque depende de decisao
# comercial (cor anunciada e preco). Fica no disco, fora de public/ — dentro de
# public/ o gerador de medidas a indexava e um `git add -A` a publicaria com a
# cor errada. Ver "E. Bloqueios humanos" em FINAL_CONSOLIDATED_AUDIT.md.
midia-aguardando-definicao/
